
public class fighter {

	
	int [] swordStats = {10, 50, 1, 75, 50, 0}; 
    // element 0 = min damage (10)
    // element 1 = max damage (60)
    // element 2 = optimal distance  (1)
    // element 3 = optimal distance chance to hit (75%)
    // element 4 = sub optimal chance to hit (0%)
    // Note: The sword can only hit at close range
	 
	int[] spearStats = {20, 40, 3, 80, 20}; 
	    // element 0 = min damage (20)
	    // element 1 = max damage (40)
	    // element 2 = optimal distance  (3)
	    // element 3 = optimal distance to hit (80%)
	    // element 4 = Sub optimal distance for out of range (20%)
	    // Note: the spear can hit at any range.
 	
	int actionPoints; 
	
	
}
